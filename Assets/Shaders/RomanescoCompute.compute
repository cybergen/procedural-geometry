#pragma kernel CSMain

RWTexture2D<float4> Displace;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{	
    float4 positionOffset = float4(1, 1, 1, 1);
    Displace[id.xy] = positionOffset / 2 + 0.5;
}
